# main.py

tasks = []

def add_task(task):
    tasks.append(task)
    print(f"Tarefa '{task}' adicionada.")

def list_tasks():
    for i, task in enumerate(tasks, 1):
        print(f"{i}. {task}")

# Testando
add_task("Estudar Git")
add_task("Fazer exercício de programação")
list_tasks()
